Console.write("How many items? ");
var response = Console.readLine();
var count = number(response) or 5;

var array = Array(count);

for (var i = 0; i < array.length(); i = i + 1) {
    array[i] = i * i;
}

print array;

array.foreach(fun (e, i) {
    print i + 1 + ": " + e;
});
